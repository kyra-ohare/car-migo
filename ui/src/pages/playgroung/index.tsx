import { Tooltip, Button } from "@mui/material";
import DeleteIcon from "@mui/icons-material/Delete";

export default function Playground() {
  return (
    <Tooltip title="Delete">
      <Button variant="contained">boo</Button>
    </Tooltip>
  );
}
